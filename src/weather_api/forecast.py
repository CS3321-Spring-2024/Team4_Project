# This is the main file for creating the forecast JSON files
# It should ideally contain functions that call the other modules for their respective tasks and then create the JSON files
# These will then be used by the __init__.py for the API to serve the forecast data

# scrath that. I think this file should just be deleted
#
